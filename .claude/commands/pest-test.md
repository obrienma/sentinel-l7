Write a Pest test for $ARGUMENTS.
Use dataset-driven tests where the input space is non-trivial.
Mock external dependencies at the service boundary, not inside the class.
Do not test implementation details — test behaviour and output.
Assert on structured log output where observability matters.